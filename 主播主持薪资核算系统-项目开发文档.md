# 多岗位员工薪资与业绩核算系统项目开发文档

## 1. 文档目标

本文档用于指导多岗位员工薪资与业绩核算系统的设计、开发、测试和部署。前端继续使用现有 Next.js 静态导出工程，当前版本只实现 Supabase 后端方案。前端保留独立 API 层以隔离 Supabase 细节，为未来迁移其他后端提供边界，但本期不开发或维护 Next.js 全栈适配器。

## 2. 项目范围

系统用户分为管理员和普通用户。普通用户职位包括主持、主播、舞蹈老师、化妆师及管理员后续创建的其他职位。管理员负责设置职位、权限、工资计算方式和业务数据；普通用户可查看自己的业绩与工资条，其中主持可在获得对应权限后上传其负责主播的业绩。

核心原则：

- 管理员拥有系统全部权限；普通用户采用“职位默认权限 + 用户额外授权”的细粒度权限模型。
- 职位由管理员维护，职位不直接等同于系统角色；系统基础角色只分管理员和普通用户。
- 管理员端首期只建设 Web 端，以桌面宽屏操作为主。
- 普通用户端移动端优先，同时兼容桌面浏览器。
- 普通用户工资计算方式由管理员配置，可按职位设置默认方案，也可按用户单独覆盖。
- 薪资最终结果由可信后端或数据库函数计算，浏览器只用于录入和预览。
- 金额统一按“分”保存为整数，比例统一按基点保存，避免浮点误差。
- 月度工资确认后保存计算规则及输入快照，防止规则调整影响历史记录。
- 普通用户默认只能查看本人数据，额外业务能力必须由管理员明确授权。

## 3. 当前工程现状

当前项目为 Next.js 16、React 19、TypeScript 5 和 Tailwind CSS 4 的 App Router 工程。页面仍是初始化模板，业务代码尚未开始。TypeScript 已启用严格模式。

当前 Next.js 配置启用了 `output: "export"`，构建产物为静态文件，不能运行 Route Handlers、Server Actions 等 Next.js 服务端能力。本期保持该配置，并使用 Supabase 提供认证、数据访问、RLS、RPC、存储及必要的 Edge Function。

## 4. 总体架构方案

### 4.1 当前实施方案：Next.js 静态网页 + Supabase


```text
浏览器中的 Next.js 静态页面
  -> 前端统一 API 层
  -> Supabase Auth
  -> Supabase Data API / RPC
  -> PostgreSQL + Row Level Security
  -> Storage / Edge Function
```

技术栈：

- Web：Next.js、React、TypeScript、Tailwind CSS。
- 构建：继续使用静态导出，产物位于 `out` 目录。
- 后端服务：Supabase Auth、PostgreSQL、RLS、RPC、Storage、Edge Function。
- 数据访问：由前端 API 层封装 Supabase JavaScript SDK。
- 数据请求：TanStack Query 调用 API 层，不直接调用 Supabase SDK。
- 参数校验：Zod。
- 导出：小规模非敏感数据可在前端生成 Excel；敏感或大批量导出使用 Edge Function。

方案优势：

- 与当前静态导出配置匹配，部署和运维成本较低。
- 登录、数据库、备份和数据 API 可快速落地。
- PostgreSQL RLS 可在数据库层强制隔离普通用户本人数据，并限制主持只能操作其负责主播的业绩。
- 统一 API 层隔离平台 SDK，未来迁移后端时无需大范围修改页面和业务组件。

实施约束与风险：

- 当前实现对 Supabase 存在依赖，但业务类型不得直接暴露 Supabase SDK 返回类型。
- 复杂事务、批处理和定制后端流程必须使用 PostgreSQL RPC 或 Edge Function。
- RLS 配置错误可能导致严重越权，必须为每条策略编写允许与拒绝测试。
- 薪资保存、确认、发布、冲正等敏感操作必须在可信环境重新鉴权和计算，不能采信浏览器提交的结果。
- 本期不创建 Next.js Route Handlers、Server Actions、Prisma 数据层或 Next.js 后端适配器。

## 5. 用户、职位与权限模型

### 5.1 身份模型

- 系统角色：`admin`（管理员）与 `user`（普通用户）。
- 业务职位：主持、主播、舞蹈老师、化妆师及其他自定义职位。
- 一名普通用户至少关联一个职位；允许多职位。
- 管理员拥有全部权限，不受职位权限限制。

### 5.2 授权模型

采用 RBAC 与用户级授权结合的方式：

1. 管理员创建职位并设置该职位的默认权限。
2. 普通用户继承所属职位的默认权限。
3. 管理员可对单个用户追加或拒绝部分权限；有效权限按“职位默认权限并集 + 用户允许项 - 用户拒绝项”计算，用户拒绝优先。
4. 数据范围与操作权限分开控制，例如“上传业绩”不代表“查看全部员工工资”。
5. 普通用户的数据范围默认为本人；主持上传主播业绩时，仅能操作与自己建立负责关系的主播。

建议权限编码：

- `self.performance.read`：查看本人业绩。
- `self.payslip.read`：查看本人工资条。
- `anchor.performance.create`：上传负责主播业绩。
- `anchor.performance.update`：修改尚未确认的主播业绩。
- `team.performance.read`：查看授权团队业绩。
- `employee.manage`：管理员工、职位和归属。
- `salary.rule.manage`：配置工资计算方式。
- `salary.calculate`：执行工资核算。
- `salary.confirm`：确认工资。
- `salary.export`：导出工资明细。
- `permission.manage`：管理职位和用户授权。
- `audit.read`：查看审计日志。

### 5.3 各职位默认权限

以下为系统初始化时的建议默认权限。管理员可修改职位默认权限，也可通过用户级允许或拒绝授权覆盖；新增职位默认只拥有普通用户基础权限。

#### 5.3.1 管理员

- 管理员工、职位、人员关系及账号状态。
- 配置职位权限和用户级授权。
- 查看、录入、审核和修正全部业绩。
- 创建、版本化、分配工资方案并执行工资核算。
- 确认、发布、作废、冲正和导出工资记录。
- 查看全部工资条、统计报表和审计日志。

管理员拥有系统全部权限，但所有敏感操作仍必须记录审计日志。

#### 5.3.2 主持

默认权限：

- 查看和维护本人资料。
- 查看本人业绩、历史记录和工资条。

可由管理员授予：

- 查看授权团队及负责主播的业绩。
- 创建、修改和提交负责主播尚未审核的业绩。
- 查看授权范围内的业绩汇总。

主持默认不能查看他人工资条、审核自己提交的业绩、配置或确认工资、管理权限及审计日志。

#### 5.3.3 主播

默认权限：

- 查看和维护本人资料。
- 查看本人业绩、历史记录和工资条。

可由管理员授予补充或提交本人业绩材料，以及查看相关审核状态和驳回原因。主播默认不能查看团队数据、上传他人业绩、查看他人工资或确认工资。

#### 5.3.4 舞蹈老师

默认权限：

- 查看和维护本人资料。
- 查看本人课时、业绩或考核记录及工资条。

可由管理员授予提交本人课时、排练或考核材料，以及查看授权班组或课程汇总。默认不能查看他人工资、配置工资规则或确认工资。

#### 5.3.5 化妆师

默认权限：

- 查看和维护本人资料。
- 查看本人服务单、业绩或考核记录及工资条。

可由管理员授予提交本人服务次数、服务单或证明，以及查看授权项目或班组汇总。默认不能查看他人工资、配置工资规则或确认工资。

#### 5.3.6 其他自定义职位

- 默认仅可查看和维护本人资料、本人业绩及本人工资条。
- 管理员从权限清单中设置职位默认权限，不能因职位名称自动获得权限。
- 涉及他人数据时必须同时配置操作权限和数据范围。
- 工资核算、工资确认、权限管理和审计查看首期仅管理员拥有。

权限必须在可信端校验。隐藏菜单和按钮不能替代服务端 RBAC 或数据库 RLS。

### 5.4 终端与响应式策略

- 管理员端仅建设 Web 端，优先适配 1280px 以上桌面屏幕，重点支持复杂表格、批量操作和报表导出。
- 普通用户端采用移动端优先设计，主要页面包括首页概览、本人业绩、工资条、个人资料和授权业务入口。
- 主持的主播业绩上传页面优先适配手机单手操作，并支持草稿、校验和提交状态反馈。
- 管理端和普通用户端共享组件与领域逻辑，但使用不同布局和导航。

## 6. 工资与业绩计算设计

### 6.1 可配置计算方式

管理员可以创建、启停和版本化工资计算方案，并设置职位默认方案或指定用户方案。首期建议支持以下规则组件：

- 固定底薪。
- 业绩金额乘固定比例。
- 按业绩区间匹配阶梯比例。
- 达标后发放固定奖励或保底。
- 固定金额、比例及自定义项目扣款。
- 多项结果相加减的组合公式。

规则使用结构化配置保存，不允许管理员直接输入并执行 JavaScript 或 SQL。计算顺序、舍入方式、生效时间和封顶值必须明确。工资生成时保存规则版本和配置快照。

### 6.2 主播默认工资模板

主播可默认采用原需求中的“保底 + 超额阶梯提成”模板，管理员可为个人调整保底或分配其他计算方案：

- 保底达标流水：`保底 × 1.65 + 保底`。
- 提成起征流水：`保底 ÷ 20%`。
- 提点从 20% 起，流水每增加 10000 元增加 1%，最高 25%。
- 平台服务费：`实际流水 × 3%`。
- 实发工资：`达标保底 + 超额提成 - 平台服务费 - 扣款合计`。

### 6.3 主持默认工资模板

| 团队流水 | 新主持 | 老主持 |
| --- | --- | --- |
| 少于 25 万 | 0% | 0% |
| 25 万至不足 35 万 | 5% | 6% |
| 35 万至不足 45 万 | 6% | 7% |
| 45 万及以上 | 7% | 8% |

主持可被授权上传负责主播的业绩，但上传行为与工资确认权限分离。上传记录先进入草稿或待审核状态，由管理员审核后参与正式核算。

### 6.4 其他职位工资

舞蹈老师、化妆师及新增职位不在代码中写死公式。管理员为其选择固定工资、业绩提成、阶梯、奖励、扣款或组合模板。若某用户没有有效计算方案，系统不得自动生成正式工资，应提示管理员补充配置。

### 6.5 上线前必须确认

1. 主播未达到保底门槛时的工资算法。
2. 超额提成基数是全部流水还是超过起征线的部分。
3. 服务费是直接扣工资，还是先扣流水再计算提成。
4. 金额舍入规则及负数工资是否归零。
5. 一个用户是否允许关联多个职位，以及多职位工资如何合并。
6. 主持上传主播业绩后是否需要管理员逐条审核。

所有工资记录应保存 `formulaVersion`、规则配置快照、输入快照、中间结果和最终结果。已确认月份只允许冲正，不应直接覆盖。

## 7. 数据模型

建议核心表：

- `profiles`：账号、姓名、系统角色、状态及联系方式。
- `positions`：职位名称、说明、状态和默认工资方案。
- `user_positions`：用户与职位关系、生效和失效日期。
- `permissions`：权限编码及说明。
- `position_permissions`：职位默认权限。
- `user_permission_overrides`：用户级允许或拒绝授权。
- `employee_relations`：主持与其负责主播等人员业务关系。
- `performance_records`：人员、月份、业绩类型、金额、录入人、审核状态和附件。
- `salary_schemes`：计算方案名称、适用职位、版本、生效时间及状态。
- `salary_scheme_rules`：规则类型、顺序、条件、参数、舍入及封顶配置。
- `salary_records`：用户、月份、职位快照、规则快照、输入、计算明细、扣款、实发和状态。
- `payslips`：工资条编号、工资记录、发布时间和用户查看时间。
- `audit_logs`：操作人、操作类型、资源、变更前后数据和时间。

关键约束：

- 工资记录按“用户 ID + 月份 + 核算批次”建立唯一约束。
- 金额使用 64 位整数分值，禁止使用浮点数。
- 业绩状态采用 `draft`、`pending`、`approved`、`rejected`。
- 工资状态采用 `draft`、`confirmed`、`published`、`voided`。
- 已生效的工资方案禁止原地修改，调整时创建新版本。
- 财务记录禁止物理删除，采用停用、作废或冲正。

## 8. 接口与数据访问

前端必须设置独立 API 层，所有网络请求统一由该层发起。页面、组件、Hooks 和状态管理代码不得直接调用 `fetch`、Axios、Supabase SDK、RPC 或 Edge Function，以隔离具体后端实现，方便后续在 Next.js 全栈、Supabase 或其他服务之间迁移。

API 层按业务域暴露稳定的方法和类型，建议包含以下能力：

- 登录、退出、获取当前用户、职位和有效权限。
- 管理用户、职位、人员关系、职位默认权限和用户单独授权。
- 创建、版本化和分配工资计算方案。
- 普通用户查询本人业绩和工资条。
- 主持上传、修改和提交负责主播的业绩。
- 管理员审核业绩、执行工资试算、确认、发布和冲正。
- 按月份、职位、用户和状态查询记录。
- 导出授权范围内的工资与业绩明细。
- 查询审计日志。

API 层分为三部分：

- `contracts`：定义请求参数、响应数据、分页结构和业务错误类型，不引用具体后端 SDK 类型。
- `client`：提供统一认证头、超时、取消请求、响应解析和错误转换能力；底层可使用原生 `fetch`，需要拦截器或上传进度时可替换为 Axios。
- `adapters`：本期只实现 Supabase Data API、RPC、Storage 和 Edge Function 适配器，对上层提供与平台无关的方法签名。

页面只调用 API 层，TanStack Query 的 `queryFn` 和 `mutationFn` 也只能调用 API 层方法。迁移后端时原则上只替换 `client` 或 `adapters`，不修改页面和业务组件。API 层只负责通信协议、数据转换与错误归一化；工资计算、权限判断等核心规则仍放在领域层和可信后端执行。

Supabase 本人数据查询由 API 层适配器访问受 RLS 保护的表或视图；用户授权、业绩审核、规则管理、工资计算、确认、发布、冲正和敏感导出必须由适配器调用 PostgreSQL RPC 或 Edge Function。

统一返回可识别的业务错误码，例如：`UNAUTHENTICATED`、`FORBIDDEN`、`OUT_OF_SCOPE`、`INVALID_INPUT`、`SALARY_SCHEME_MISSING`、`DUPLICATE_MONTH`、`RECORD_CONFIRMED` 和 `FORMULA_ERROR`。

## 9. 工程目录建议

```text
app/
  (auth)/login/
  (admin)/employees/
  (admin)/positions/
  (admin)/permissions/
  (admin)/performance/
  (admin)/salary-schemes/
  (admin)/payroll/
  (user)/home/
  (user)/performance/
  (user)/payslips/
  (user)/anchor-performance/    # 有授权时显示
components/
features/
  auth/
  employees/
  permissions/
  performance/
  salary-schemes/
  payroll/
  payslips/
lib/
  api/
    contracts/                  # 与后端无关的请求、响应和错误类型
    client/                     # 请求客户端、认证、超时和错误归一化
    adapters/                   # 本期仅实现 Supabase 适配器
    auth.ts                     # 按业务域暴露稳定 API
    employees.ts
    performance.ts
    payroll.ts
    payslips.ts
  domain/payroll/               # 规则执行器、纯计算函数和类型
  authorization/                # 权限展示与前端操作可用性判断
  validations/
  supabase/                     # 仅供 API 适配器引用
supabase/                       # 数据库迁移、RLS、RPC 与 Edge Function
tests/
```

前端业务代码统一依赖 `lib/api` 的稳定接口，不依赖 Supabase SDK 返回类型或数据库模型。本期只保留 Supabase 适配器，不实现备用后端；未来迁移时新增或替换适配器，并通过契约测试验证行为一致。

## 10. 前端状态管理

- 管理员端和普通用户端均在客户端通过统一 API 层查询数据。
- 普通用户端优化首屏体积、弱网加载、触摸区域和移动端表格展示。
- 异步服务端数据、缓存、刷新和请求去重使用 TanStack Query，其查询与变更函数不得绕过 API 层。
- API 层默认可使用原生 `fetch`；只有需要拦截器、上传进度等功能时再引入 Axios，并保持上层调用接口不变。
- 表单使用 React Hook Form 与 Zod。
- 当前有效权限登录后加载并缓存；后端仍需对每次操作重新鉴权。
- 当前没有复杂跨模块客户端状态，不建议引入 Redux。

## 11. 安全设计

- 管理员与普通用户使用系统角色控制基础边界，职位权限和用户级覆盖决定具体操作能力。
- 每次请求同时校验“操作权限”和“数据范围”，不能只校验职位名称。
- Supabase 为用户、业绩、工资条、人员关系和规则表启用 RLS；普通用户仅能读取本人数据，主持仅能写入其负责主播且尚未确认的业绩。
- 浏览器只使用 Supabase 匿名公钥，`service_role` 密钥只能放在可信服务环境。
- 工资规则只允许结构化配置，禁止执行管理员提交的任意脚本或 SQL。
- 登录采用安全 Cookie 或 Supabase 官方会话机制，生产环境强制 HTTPS。
- API、RPC 和表单统一使用 Zod 或数据库约束校验。
- 权限、工资规则、业绩、工资确认和导出变更全部写入审计日志。
- 日志避免记录密码、令牌及不必要的完整工资数据。
- 数据库执行自动备份，并定期演练恢复。

## 12. 测试策略

### 12.1 单元测试

使用 Vitest 覆盖：

- 固定工资、比例、阶梯、奖励、扣款和组合规则。
- 各阶梯临界值前后 1 分、封顶、舍入和负数处理。
- 主播 20% 至 25% 提点与主持 25 万、35 万、45 万边界。
- 工资规则版本切换后历史快照保持不变。
- 职位默认权限和用户允许/拒绝覆盖的合并逻辑。

### 12.2 集成与权限测试

- 管理员可管理用户、职位、权限、规则和全部业务数据。
- 普通用户只能读取本人的业绩和工资条。
- 主持只有获得权限后才能上传业绩，且只能操作其负责主播。
- 上传业绩不能直接取得工资确认或查看他人工资的权限。
- 缺少有效工资方案时禁止生成正式工资。
- 已确认或已发布记录不能被普通修改。
- Supabase 每条 RLS 策略必须有允许与拒绝用例。

### 12.3 端到端测试

使用 Playwright 分别覆盖桌面管理员端和移动端普通用户流程，包括登录、职位授权、规则配置、业绩上传与审核、工资试算、确认发布、本人业绩、工资条查询和导出。上线前以人工核算样本逐笔对账。

## 13. 部署与环境

### 13.1 静态 Next.js + Supabase

- 保留静态导出配置，构建后部署 `out` 目录至静态托管平台。
- Supabase 分别建立开发、测试和生产项目。
- 通过迁移文件管理表、索引、函数和 RLS，禁止只在控制台手工修改生产库。
- 环境变量仅公开 Supabase URL 和匿名公钥；服务角色密钥不得进入前端环境变量。

## 14. 开发阶段计划

1. 确认公式歧义、多职位、业绩审核和工资发布规则。
2. 配置 Supabase 开发、测试和生产环境，补充依赖与环境变量。
3. 建立用户、职位、权限、人员关系、工资方案和审计数据模型。
4. 实现权限合并、数据范围控制和完整权限测试。
5. 实现结构化工资规则执行器及边界单元测试。
6. 实现管理员 Web 端的员工、职位、授权和工资方案管理。
7. 实现移动端优先的普通用户首页、本人业绩和工资条。
8. 实现主持上传主播业绩及管理员审核流程。
9. 实现工资试算、确认、发布、历史查询、冲正和导出。
10. 完成 RLS、集成、响应式和端到端测试。
11. 完成数据备份、部署、样本对账和用户验收。

## 15. 验收标准

- 管理员可创建职位、设置默认权限、调整个人权限并配置工资计算方式。
- 管理员端在桌面浏览器完成全部管理操作，普通用户核心流程满足移动端使用。
- 普通用户只能查看本人的业绩和工资条。
- 获得授权的主持只能上传其负责主播的业绩，不能确认工资或越权读取数据。
- 所有已配置工资方案的样本结果与人工核算一致；无方案用户不会生成正式工资。
- 金额计算无浮点误差，历史记录不因规则升级改变。
- 所有关键授权、规则、业绩、工资和导出操作可审计。
- 生产构建、自动化测试、备份和恢复流程可重复执行。

## 16. 主要风险

- 财务口径、多职位和审核流程未确认是上线阻塞项。
- 动态工资规则若允许执行任意代码会形成高危漏洞，必须限制为受控规则组件。
- 职位权限、用户覆盖和数据范围组合复杂，应避免仅靠前端菜单控制权限。
- RLS 遗漏可能造成业绩或工资泄露，应在接入真实数据前完成专项测试。
- 仅在前端计算会被篡改，正式落库必须由可信端重新计算。
- 修改规则但不保存版本快照会造成历史漂移。
- 静态导出与 Next.js 服务端能力互斥，选型后必须同步调整配置、部署和代码边界。