# 主播主持薪资核算系统 — 实施计划

> 本文档依据《主播主持薪资核算系统-项目开发文档》制定，用于指导落地开发。
> 编制日期：2026-09-08。当前状态：**规划阶段，暂不编码**。

## 1. 计划概述

- 技术栈：Next.js 16 静态导出 + React 19 + TypeScript 5 + Tailwind 4；后端 Supabase（Auth / PostgreSQL / RLS / RPC / Storage / Edge Function）。
- 架构原则：前端统一 API 层隔离 Supabase SDK；金额按“分”存整数、比例按基点存；工资以可信端计算为准；已确认月份只冲正不覆盖。
- 数据请求 TanStack Query，校验 Zod，表单 React Hook Form，测试 Vitest + Playwright。
- 当前工程为初始化模板，业务依赖（Supabase / TanStack Query / Zod / RHF 等）尚未安装。

## 2. 上线前规则确认状态

| 序号 | 规则 | 状态 | 结论 |
| --- | --- | --- | --- |
| 1 | 主播工资算法（基本/保底/绩效/达标门槛） | 已确认 | **前3月拿满基本工资(无责)；第4月起达标=基本+绩效、不达标=保底；达标门槛=基本×2.65** |
| 2 | 超额提成基数（全部流水 / 仅超起征线部分） | 已确认 | **按全部月流水计提；达标后整月流水（含门槛内部分）均参与提成** |
| 3 | 服务费扣除方式 | 已确认 | **所有人总工资一律扣3%（抖音提现手续费），服务费向上取整到分，实发=总工资−服务费** |
| 4 | 金额舍入 / 负数工资 | 已确认 | **不归零，按实际计算结果显示** |
| 5 | 多职位与工资合并 | 已确认 | **允许一人多职位；多职位工资由管理员单独设置，不自动合并** |
| 6 | 主持上传主播业绩审核 | 已确认 | **不需逐条审核；管理员可审核并驳回，被驳回数据需相关人重新上传** |

> 规则 2（超额提成基数）**已确认**：按全部月流水计提，达标后整月流水（含门槛内部分）均参与提成。6 项上线前规则已全部确认，阶段5 工资引擎无阻塞可开工。

## 3. 已确认规则对设计的影响

- **规则1（主播工资模型）**：管理员为每位主播设置**基本工资**（示例 8000）与**保底工资**（示例 5000）。**达标门槛**（基本达标流水）= 基本工资 × 1.65 + 基本工资 = 基本工资 × 2.65（如 8000→21200）。判定：当月流水 ≥ 门槛为"达标"，否则"不达标"。发放规则按在职月序区分：
  - **前 3 个月（第1~3月，无责期）**：达标 → 实发 = 基本工资 + 绩效工资；不达标 → 实发 = 基本工资（无责保底，不降为保底工资）。
  - **第 4 个月起**：达标 → 实发 = 基本工资 + 绩效工资；不达标 → 实发 = 保底工资。
  - **绩效工资 = 超额提成**：按**全部月流水**计提（达标后整月流水含门槛内部分均参与提成，规则2已确认）。基本/保底工资金额配置化（按人设置，非硬编码）；月序按入职日期运行时计算。
- **规则3（服务费=提现手续费）**：服务费为抖音平台提现手续费，对**所有人**的总工资一律扣 3%，含按保底发放者。计算：`服务费 = ceil(总工资 × 3%)`（**向上取整到分**，按平台实际手续费）；`实发 = 总工资 − 服务费`。金额按分存整数，3% 以基点表示（300 bps）。
- **规则4（不归零）**：`salary_records` 允许保存负数实发；计算引擎不做 `max(0, x)` 截断；UI 需明确展示负数并做视觉提醒。
- **规则5（多职位单独设置）**：`user_positions` 支持多条；工资方案分配以“用户+职位”为粒度；不实现自动加总合并逻辑，改由管理员为每个职位分别核算并展示。
- **规则6（驳回重传）**：业绩状态机为 `draft → pending → approved`，新增 `rejected`；`rejected` 记录允许原上传人重新编辑并再次提交（回到 `pending`），需保留驳回原因字段与审计。

## 4. 阶段划分与任务

### 阶段 0 · 规则确认（部分完成）
- [x] 收敛规则 4/5/6。
- [x] 收敛规则 1（主播基本/保底/绩效三层模型，达标门槛=基本×2.65，前3月无责）、规则 3（服务费3%向上取整）。
- [x] 确认规则 2（超额提成基数）：**按全部月流水计提，达标后整月流水均参与提成**。

### 阶段 1 · 环境与工程骨架（已完成 2026-09-08）
- [x] 安装依赖：`@supabase/supabase-js`、`@tanstack/react-query`、`zod`、`react-hook-form`、`@hookform/resolvers`；开发依赖 `vitest`、`@playwright/test`（均已装）。
- [x] `.env` 接入预留：`.env.example`（含 URL/匿名公钥/service_role 三项及安全注释）+ `.env.local`（占位待填），`.gitignore` 放行 `.env.example`。
- [x] 目录骨架与占位模块落地：`app/(auth)/login`、`app/(admin)/admin`、`app/(user)/dashboard`；`lib/supabase/{env,client}.ts`、`lib/api/{contracts,client,adapters}`、`lib/api/{auth,employees,performance,payroll,payslips}.ts`、`lib/domain/payroll`、`lib/authorization`、`lib/validations`；`components/`、`features/`、`supabase/migrations/`、`tests/` 占位；`app/providers.tsx` 挂载 TanStack Query。
- [x] `pnpm build` 通过（生成 /、/admin、/dashboard、/login 路由）。
- 备注：Supabase 真实密钥待用户提供后填入 `.env.local`；env 读取为惰性校验，构建不受空值阻塞。

### 阶段 2 · 数据模型与 RLS
- 编写迁移文件建核心表：`profiles`、`positions`、`user_positions`、`permissions`、`position_permissions`、`user_permission_overrides`、`employee_relations`、`performance_records`、`salary_schemes`、`salary_scheme_rules`、`salary_records`、`payslips`、`audit_logs`。
- 约束：金额 64 位整数分值；业绩状态 `draft/pending/approved/rejected`；工资状态 `draft/confirmed/published/voided`；工资唯一约束（用户+月份+批次）；已生效方案禁改（建新版本）；财务记录禁物理删除。
- 为各表配置 RLS：普通用户仅读本人数据；主持仅能写其负责且未确认的主播业绩；每条策略配套允许+拒绝测试用例。
- 交付：迁移脚本 + RLS 策略 + RLS 测试样例。

### 阶段 3 · 统一 API 层
- `contracts`：请求/响应/分页/业务错误码类型（`UNAUTHENTICATED`/`FORBIDDEN`/`OUT_OF_SCOPE`/`INVALID_INPUT`/`SALARY_SCHEME_MISSING`/`DUPLICATE_MONTH`/`RECORD_CONFIRMED`/`FORMULA_ERROR`），不引用 SDK 类型。
- `client`：统一认证头、超时、取消、响应解析、错误归一化（默认 fetch，必要时替换 Axios）。
- `adapters`：Supabase Data API / RPC / Storage / Edge Function 适配器，本人数据走受 RLS 保护表；授权、审核、规则、工资计算/确认/发布/冲正/敏感导出走 RPC 或 Edge Function。
- 业务域 API：`auth.ts`、`employees.ts`、`performance.ts`、`payroll.ts`、`payslips.ts`。
- 交付：页面/组件/Hooks 一律经 API 层访问后端，无直接 SDK 调用。

### 阶段 4 · 权限模型
- 实现权限合并：职位默认权限并集 + 用户允许 − 用户拒绝（拒绝优先）。
- 数据范围与操作权限分离控制；主持数据范围限定其负责主播。
- 交付：权限合并纯函数 + 完整权限测试（含越权拒绝用例）。

### 阶段 5 · 工资规则执行器（进行中 2026-09-08：主播计算器已交付）
- 结构化规则组件：固定底薪、比例提成、阶梯提成、达标奖励/保底、扣款、组合公式；不执行任意脚本/SQL。
- 主播默认模板（保底+超额阶梯提成，20%→25%，服务费）、主持团队阶梯模板（新/老主持，25/35/45 万边界）。
- 已确认口径落地：
  - 主播工资模型（规则1）：管理员按人设基本/保底工资；达标门槛=基本×2.65；前3月达标发基本+绩效、不达标发基本(无责)；第4月起达标发基本+绩效、不达标发保底。
  - 服务费（规则3）：对所有人 `服务费 = ceil(总工资 × 3%)` 向上取整到分，`实发 = 总工资 − 服务费`；3% 以 300 bps 表示。
  - 超额提成基数（规则2）**已确认**：按全部月流水计提；绩效工资 = 全部月流水 × 提成费率（达标后整月流水含门槛内部分均参与）。
- 遵循规则4：不做负数归零；保存 `formulaVersion` + 规则快照 + 输入快照 + 中间/最终结果。
- 交付：执行器 + 边界单元测试（临界值±1 分、封顶、舍入向上取整、达标门槛±1分、前3月/第4月发放切换边界、达标/不达标切换、负数）。

- **已交付（2026-09-08）**：
  - `lib/domain/payroll/types.ts`：`AnchorSalaryScheme`/`AnchorPayrollInput`/`AnchorPayrollResult` 类型与默认服务费费率、无责期常量。
  - `lib/domain/payroll/money.ts`：`applyRateCeil`（向上取整）/`applyRateFloor`（向下取整）/`isWithinGracePeriod`，整数运算防浮点误差。
  - `lib/domain/payroll/anchor.ts`：`calculateAnchorPayroll` 主播工资计算器，落地规则 1/2/3/4，非法输入抛 `ApiError`。
  - `tests/anchor-payroll.test.ts`：12 项边界单元测试全部通过（门槛±1、无责期、第4月降保底、服务费向上取整、负数不归零、非法月序校验）。
  - `vitest.config.*` + `package.json` 新增 `test`/`test:watch` 脚本；`pnpm build` 与 `pnpm test` 均通过。
- **待办**：主持团队阶梯模板计算器；规则快照/版本化持久化（依赖阶段2数据表）；组合公式受控组件。

### 阶段 6 · 管理员 Web 端（桌面优先）
- 员工管理、职位与默认权限、用户级授权、工资方案创建/版本化/分配（按用户+职位粒度，支撑规则5）。
- 交付：桌面宽屏管理页面（≥1280px），支持复杂表格与批量操作。

### 阶段 7 · 普通用户端（移动优先）
- 首页概览、本人业绩、工资条、个人资料、授权业务入口。
- 交付：移动优先页面，兼容桌面；负数工资明确展示（规则4）。

### 阶段 8 · 主播业绩上传与审核（规则6）
- 主持上传/修改/提交负责主播业绩（草稿→待审核）。
- 管理员审核可通过或驳回；驳回带原因，`rejected` 数据由原上传人重新编辑再提交。
- 交付：上传流程 + 审核/驳回/重传闭环 + 审计记录。

### 阶段 9 · 工资核算流程
- 工资试算、确认、发布、历史查询、冲正、导出。
- 已确认/已发布记录禁普通修改，只允许冲正；敏感/大批量导出走 Edge Function。
- 交付：核算全流程 + 冲正与导出。

### 阶段 10 · 测试
- RLS 允许/拒绝、集成权限、响应式；Playwright 覆盖桌面管理端与移动用户端全流程。
- 上线前以人工核算样本逐笔对账。

### 阶段 11 · 部署与验收
- 静态导出部署 `out` 目录至托管平台；Supabase 用迁移管理，禁控制台直改生产库。
- 数据备份与恢复演练、样本对账、用户验收。

## 5. 里程碑与依赖

```text
阶段0 规则确认(全部已确认)
  ├─ 阶段1 环境骨架 ──┐
  ├─ 阶段2 数据模型+RLS ─┤
  │                     ├─ 阶段3 API层 ─┬─ 阶段6 管理员端
  │                     │               ├─ 阶段7 用户端
  │                     └─ 阶段4 权限 ──┤
  └─ 阶段5 工资引擎 ──────────────────────┼─ 阶段8 业绩审核
                                         └─ 阶段9 工资核算
阶段6/7/8/9 ─ 阶段10 测试 ─ 阶段11 部署验收
```

- 6 项上线前规则已全部确认，阶段5 工资引擎无阻塞。各阶段可按依赖并行推进。

## 6. 主要风险

- 财务口径规则（规则1主播工资模型、规则2超额提成基数、规则3服务费）已全部确认；后续如业务口径调整需同步刷新规则版本快照。
- 动态规则若允许执行任意代码为高危漏洞，限定受控规则组件。
- RLS 遗漏可能导致业绩/工资泄露，接入真实数据前完成专项测试。
- 前端计算可被篡改，正式落库须可信端重算。
- 规则改动未保存版本快照会造成历史漂移。

## 7. 规则确认清单（全部已确认）

- 规则1、2、3、4、5、6 均已确认，无遗留阻塞项。

> 已确认摘要：规则1（主播基本/保底/绩效三层模型；达标门槛=基本×2.65；前3月无责——达标发基本+绩效、不达标发基本；第4月起达标发基本+绩效、不达标发保底）；规则2（绩效工资=超额提成，按全部月流水计提，达标后整月流水含门槛内部分均参与提成）；规则3（所有人服务费=ceil(总工资×3%)向上取整到分，实发=总工资−服务费）。